var nome = Daiane;

texto = `Eu amo ${nome} e sinto ${"amor".toLocaleUpperCase()}`;


lista =["a", "b", "c"];

const cb = (emoji) => emoji + emoji;

const texto = `emoji duplicados ${lista.map(cb)}`;

console.log(texto)
